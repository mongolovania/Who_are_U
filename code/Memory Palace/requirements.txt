# ============================================================
# Memory Palace Python Dependencies
# 安装: pip install -r requirements.txt
# ============================================================

# MCP protocol SDK (Claude communication)
mcp>=1.0.0

# FastAPI + server
fastapi>=0.110.0
uvicorn>=0.29.0

# Auth
pyjwt>=2.8.0
pydantic>=2.0.0

# Fuzzy matching (memory bucket search)
rapidfuzz>=3.0.0

# OpenAI-compatible client (DeepSeek/Ollama/LM Studio/vLLM/Gemini etc.)
openai>=1.0.0

# YAML config parsing
pyyaml>=6.0

# Markdown frontmatter parsing (bucket file I/O)
python-frontmatter>=1.1.0

# Chinese text segmentation
jieba>=0.42.1

# ChromaDB vector database (Track B: ANN embedding search)
chromadb>=0.5.0

# True BM25 probabilistic retrieval (Track B: replaces token overlap)
rank-bm25>=0.2.2

# Async HTTP client
httpx>=0.27.0

# Vector similarity (import/clustering)
numpy>=1.24.0
scikit-learn>=1.2.0

# ═══════════════════════════════════════════════════════════
# Testing & Benchmarking
# ═══════════════════════════════════════════════════════════

# Test framework
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0

# Dense embeddings + vector search (benchmark Tier 1)
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0

# Faster BM25 alternative (benchmark Tier 1)
bm25s>=0.10.0

# Community memory systems for comparison (optional — need API keys)
# mem0ai>=0.1.0        # pip install mem0ai (needs MEM0_API_KEY)
# letta>=0.6.0         # pip install letta
# graphiti-core>=0.1.0 # pip install graphiti-core
# graphrag>=1.0.0      # pip install graphrag (needs LLM API key for indexing)
