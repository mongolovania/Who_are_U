import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder — will be replaced with actual widget tests
    expect(1 + 1, equals(2));
  });
}
